---
# tasks file for roles/run_docker_compose
- name: "Execution de docker compose"
  community.docker.docker_compose_v2:
    project_src: "{{ docker_compose_project_srcs }}"
    env_files:
      - mariadb.env
      - wordpress.env
    state: present
    build: always
    files:
      - compose.yml
